import {
  formatPercentage
} from 'dashboard-client/helpers/format-percentage';

module('FormatPercentageHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatPercentage(42);
  ok(result);
});
